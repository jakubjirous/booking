import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cz.jirous.jakub.ionic.angular.booking',
  appName: 'Booking',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
